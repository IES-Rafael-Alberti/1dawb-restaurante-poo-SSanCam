/**
 * Integra y gestiona las operaciones del restaurante.
 */
class sistemaGestionRestaurante(mesas: MutableList<Mesa>) {
    var mesas: MutableList<Mesa> = mesas
        set(value) {

            field = value
        }
}